#include<stdio.h>
#include<conio.h>
#include<math.h> //Mathematical functions are loaded with math.h library.

void main()
{
	float a=100;
	
	printf("A=100\n");
	printf("Square root of A is: %f\n", sqrt(a));
	//sqrt gives the square root of a value.
	printf("Sin value of A is: %f\n", sin(a));
	printf("Cos value of A is: %f\n", cos(a));
		
	getch();
}
